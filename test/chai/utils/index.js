/**
 * Created by pascal on 23/10/15.
 */
if (typeof require !== 'undefined') {
    var expect = require('chai').expect;
} else {
    var expect = chai.expect;
}

describe('Utils Validator testing', function () {
});
